/// Fora da web (testes/VM): sem player — o VozNatural cai direto no
/// fallback (voz do sistema) sem nem tentar a rede.
const suportado = false;

void tocaPcm16(List<int> pcm, int taxa) {}

void paraTudo() {}
